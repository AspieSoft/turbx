function main({runFunc, getOpt}){
  return {
    name: 'import',
    func: function(args, content, opts, level, file){

      //todo: setup import (only for basic file imports) (also support markdown, js, css, and txt imports)
      // do something different for capital first letter components

      return '';
    },
  }
}

module.exports = main;
