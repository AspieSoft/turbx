package compiler

import "bufio"

// returning true will run the "continue" function on the pre compiler loop
func compileMarkdown(reader *bufio.Reader, b *[]byte, err *error) bool {
	//todo: compile markdown while reading file

	return false
}
